Получение данные из веб-сервиса в виде структуры:

```bsl

функция ПолучитьДанныеWebСервиса(Знач АдресWebПриложения)
	
	СтруктураURI = ОбщегоНазначенияКлиентСервер.СтруктураURI(АдресWebПриложения);
	Схема = ?(ЗначениеЗаполнено(СтруктураURI.Схема), СтруктураURI.Схема, "http");
	Прокси = ПолучениеФайловИзИнтернетаКлиентСервер.ПолучитьПрокси(Схема);
	
	
	Соединение = Новый HTTPСоединение(
	СтруктураURI.Хост,
	СтруктураURI.Порт,
	, //СтруктураURI.Логин,
	,  //СтруктураURI.Пароль, 
	Прокси,
	60,
	?(НРег(Схема) = "http", Неопределено, Новый ЗащищенноеСоединениеOpenSSL),Истина);
	
	
	HttpЗапрос = Новый HTTPЗапрос(СтруктураURI.ПутьНаСервере);
	HttpЗапрос.АдресРесурса =СтруктураURI.ПутьНаСервере;
	
	Результат = Соединение.Получить(HttpЗапрос);
	
	JsonFile = Результат.ПолучитьИмяФайлаТела();
	
	Если JsonFile = Неопределено Тогда
		
		// записываем двоичные данные
		JsonFile = ПолучитьИмяВременногоФайла("json");
		
		Результат.ПолучитьТелоКакДвоичныеДанные().Записать(JsonFile);
		
	КонецЕсли;
	
	ЧтениеJSON= Новый ЧтениеJSON;
	ЧтениеJSON.ОткрытьФайл(JsonFile);
	
	json = ПрочитатьJSON(ЧтениеJSON,,"Period", ФорматДатыJSON.ISO);
	
	ЧтениеJSON.Закрыть();
	
	Возврат json;
КонецФункции 
	

```
